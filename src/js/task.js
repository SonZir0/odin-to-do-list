export default class Task {
    constructor(name, description, dueDate, priority, fromTaskList) {
        this.name = name;
        this.description = description;
        this.dueDate = dueDate;
        this.priority = priority;
        this.fromTaskList = fromTaskList;
        this.taskID = ID;
    }
}